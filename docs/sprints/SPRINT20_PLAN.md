# Sprint 20 Plan

Sprint 20 delivers (A) a browser-session conversational Copilot with explicit
General/Portfolio/Position scope, unchanged Hebrew user input, and English-only
assistant answers and (B) governed loss-control research under the constitution:
**no actionable new trade without an approved deterministic numeric loss-control
boundary**. AlphaPilot computes; AI/React only explain/display.

Phase A adds a floating Ask AI panel, bounded session history, explicit ticker
selector, direct question-focused English responses, authoritative evidence,
and controlled provider states. `Why this position?` remains.

Phase B audits Sprint 12, freezes the separate protocol in
`SPRINT20_STOP_RESEARCH_PROTOCOL.md`, runs only its closed candidate space on
the immutable Sprint 13 snapshot, and supports `NO_WINNER`. A historical winner
may only become `PAPER_FORWARD_CANDIDATE` after human review; no V1 profile is
silently changed. `ExecutionReadiness` is ACTIONABLE only with numeric backend
loss-control boundary, policy/version/provenance, and trigger semantics. Existing positions never
receive reconstructed entry risk.

No News, broker integration, LangChain/LangGraph, strategy entry change,
automatic order, arbitrary/AI/React stop, or Sprint 21 work is in scope.

The post-review hardening recognizes frozen Micho SMA150 completed-close exit as
active strategy loss control (not a broker stop) and freezes the distinct EMA
Round 2 signal-day-low family before inspection. Round 2 also supports `NO_WINNER`.
